export enum ChannelType {
    EMAIL = 'EMAIL',
    SMS = 'SMS',
    PUSH = 'PUSH',
    WEBHOOK = 'WEBHOOK',
    IN_APP = 'IN_APP'
}

export enum ProjectStatus {
    ACTIVE = 'ACTIVE',
    SUSPENDED = 'SUSPENDED',
    DISABLED = 'DISABLED'
}